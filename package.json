{
  "name": "throttle-debounce",
  "version": "0.0.1",
  "license": "MIT",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "scripts": {
    "build": "./bin/parallel 'tsc    -p tsconfig.cjs.json' 'tsc    -p tsconfig.esm.json'",
    "watch": "./bin/parallel 'tsc -w -p tsconfig.cjs.json' 'tsc -w -p tsconfig.esm.json'",
    "eslint": "eslint 'src/**/*.ts'",
    "eslint:fix": "eslint --fix 'src/**/*.ts'",
    "prettier": "prettier --write . '!dist/**/*.ts'",
    "lint": "yarn run eslint:fix && yarn run prettier"
  },
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "@typescript-eslint/eslint-plugin": "^5.40.0",
    "@typescript-eslint/parser": "^5.40.0",
    "eslint": "^8.25.0",
    "eslint-config-prettier": "^8.5.0",
    "jest": "^29.2.0",
    "jsdom": "^20.0.1",
    "lodash.throttle": "^4.1.1",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  },
  "jest": {
    "transform": {
      "\\.[jt]sx?$": [
        "ts-jest"
      ]
    }
  },
  "dependencies": {
    "ts-jest": "^29.0.3"
  }
}
